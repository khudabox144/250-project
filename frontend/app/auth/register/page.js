import RegisterForm from '@/app/components/RegisterForm';
import React from 'react';

const page = () => {
    return (
        <div>
            <RegisterForm/>
        </div>
    );
};

export default page;