import React from 'react';
import LoginForm from '../components/LoginForm';

const page = () => {
    return (
        <div>
            this is map section 
        </div>
    );
};

export default page;